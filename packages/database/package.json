{
    "name": "@contact-scraper/db",
    "version": "0.0.0",
    "scripts": {
        "db:generate": "prisma generate",
        "db:migrate": "prisma migrate dev --skip-generate",
        "db:deploy": "prisma migrate deploy"
    },
    "exports": {
        ".": "./src/index.ts"
    },
    "devDependencies": {
        "@contact-scraper/eslint-config": "workspace:*",
        "@contact-scraper/typescript-config": "workspace:*",
        "prisma": "^6.4.1"
    },
    "dependencies": {
        "@prisma/client": "^6.4.1"
    }
}