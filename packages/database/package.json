{
    "name": "@contact-scraper/db",
    "version": "0.0.0",
    "scripts": {
        "pg:generate": "prisma generate --schema=prisma/schema.pg.prisma",
        "pg:migrate": "prisma migrate dev --skip-generate --schema=prisma/schema.pg.prisma",
        "pg:depoy": "prisma migrate deploy --schema=prisma/schema.pg.prisma",
        "db:generate": "prisma generate",
        "db:migrate": "prisma migrate dev --skip-generate",
        "db:deploy": "prisma migrate deploy"
    },
    "exports": {
        ".": "./src/index.ts"
    },
    "devDependencies": {
        "@contact-scraper/config-eslint": "workspace:*",
        "@contact-scraper/config-typescript": "workspace:*",
        "prisma": "^6.4.1"
    },
    "dependencies": {
        "@prisma/client": "^6.4.1"
    }
}